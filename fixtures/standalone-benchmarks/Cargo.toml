[package]
name = "stwo-metal-standalone-benchmarks"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]
members = []

[features]
default = []
cuda-runtime = ["dep:stwo-metal"]
metal-runtime = [
  "dep:stwo-metal",
  "dep:stwo-metal-sys",
  "dep:stwo-metal-fixture-shims",
  "dep:stwo-examples",
  "stwo-metal/parallel",
]
cairo-prove = [
  "dep:stwo-metal",
  "dep:cairo-air",
  "dep:stwo-cairo-adapter",
  "dep:stwo-cairo-prover",
  "dep:stwo-cairo-common",
]

[dependencies]
itertools = { version = "0.12", default-features = false, features = ["use_alloc"] }
num-traits = { version = "0.2.19", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
stwo = { path = "../../vendor/stwo-upstream-dev-62b228e/crates/stwo", package = "stwo", default-features = false, features = ["parallel", "prover"] }
stwo-examples = { path = "../../vendor/stwo-upstream-dev-62b228e/crates/examples", default-features = false, optional = true }
stwo-constraint-framework = { path = "../../vendor/stwo-upstream-dev-62b228e/crates/constraint-framework", default-features = false, features = ["parallel", "prover"] }
stwo-metal = { path = "../../crates/stwo-metal", features = ["prover"], optional = true }
stwo-metal-sys = { path = "../../crates/stwo-metal-sys", default-features = false, optional = true }
stwo-metal-fixture-shims = { path = "../stwo-metal-fixture-shims", optional = true }
cairo-air = { path = "../../vendor/stwo-cairo/stwo_cairo_prover/crates/cairo-air", optional = true }
stwo-cairo-adapter = { path = "../../vendor/stwo-cairo/stwo_cairo_prover/crates/adapter", optional = true }
stwo-cairo-prover = { path = "../../vendor/stwo-cairo/stwo_cairo_prover/crates/prover", optional = true }
stwo-cairo-common = { path = "../../vendor/stwo-cairo/stwo_cairo_prover/crates/common", features = ["prover"], optional = true }

[patch.crates-io]
stwo = { path = "../../vendor/stwo-upstream-dev-62b228e/crates/stwo" }
stwo-constraint-framework = { path = "../../vendor/stwo-upstream-dev-62b228e/crates/constraint-framework" }
